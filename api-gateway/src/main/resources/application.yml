spring:
  application:
  name: api-gateway

server:
  port: 8081

eureka:
client:
service-url:
defaultZone: http://localhost:8761/eureka/

cloud:
gateway:
discovery:
locator:
enabled: true
lower-case-service-id: true
